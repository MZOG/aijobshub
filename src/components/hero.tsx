import Container from "./container";

const Hero = () => {
  return (
    <Container>
      <h1 className="text-4xl font-bold">Find a job in AI</h1>
      <p className="text-lg text-muted-foreground">
        Get hired in a next generation AI company right now
      </p>
    </Container>
  );
};

export default Hero;
